# Stage 81D-3.1 — Savings Release Backup & Copy Compliance Fix

**Type:** Fix (release-readiness)  
**Verdict:** Built — audit pending  
**Date:** May 2026  
**Parent:** [81D-3 savings release confirmation sheet](./81D-3-savings-release-confirmation-sheet.md)  
**Closes:** [81D-3-C post-build audit](./81D-3-savings-release-confirmation-sheet.md) items (backup whitelist, em-dash copy)

---

## Objective

Close release-readiness gaps from **81D-3-C** without changing savings release behaviour:

1. Include **`savingsReleases`** in backup/restore whitelist so release history survives export/import.
2. Remove **em dashes** from new visible Savings Release copy (style gate).

**No changes** to apply logic, preview math, net worth, Monthly Left, mutation rules, activityLog, UI entry, or protected files.

---

## Changes

| Area | Change |
|------|--------|
| **`geodeBeyndBackupRestorableKeyWhitelist()`** | Added `'savingsReleases'` after `'investments'` |
| **`geodeSavingsReleaseSourceSelectLabel`** | `Name — £X available` → `Name (£X available)` |
| **`geodeOpenSavingsReleaseModal` intro** | Em dash replaced with two short sentences |

---

## Scope

| File | Change |
|------|--------|
| `index.html` | Whitelist + copy only |
| `docs/stages/81/*`, `docs/README.md`, `docs/architecture-notes.md` | Documentation |

---

## Not changed

- `geodeApplySavingsRelease`, `geodePreviewSavingsRelease`, eligibility, linked investment rules
- Plan overbudget entry, modal fields, preview layout
- `service-worker.js`, `coaching.json`, `manifest.json`, `js/geode-pure/`

---

## Validation

Standard gate: ReadLints `index.html`, `git diff --check`, protected files unchanged, JSON/SW checks.

Manual: export backup with `savingsReleases` populated → restore → array present and normalized on load.

---

## Service worker

**No bump** — whitelist and copy only.
